{
    "name": "@ngos/admin-core-js-tests",
    "private": true,
    "description": "Vitest suite for the front-end JS shipped as stubs by ngos/admin-core (escaping, table actions, media picker).",
    "type": "module",
    "scripts": {
        "test": "vitest run"
    },
    "devDependencies": {
        "jquery": "^3.7.1",
        "jsdom": "^25.0.1",
        "vitest": "^2.1.8"
    }
}
