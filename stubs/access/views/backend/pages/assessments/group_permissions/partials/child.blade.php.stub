@if(isset($items) && count($items) > 0)
    <ol class="dd-list">
        @foreach($items as $item)
            <li class="dd-item" data-id="{{ $item->id }}">
                <div class="dd-handle">
                    <i class="bi bi-grip-vertical text-muted me-2"></i>{{ $item->name }}
                    <span class="float-end">
                        <x-admin-core::badge tone="info">{{ $item->permissions->count() }}</x-admin-core::badge> Permissions
                    </span>
                </div>
                @if(isset($item->child) && count($item->child) > 0)
                    @include('backend.pages.assessments.group_permissions.partials.child', ['items' => $item->child])
                @endif
            </li>
        @endforeach
    </ol>
@endif
