<!DOCTYPE html>
{{--
    Starter backend layout published by `php artisan admin-core:install`.
    It is intentionally self-contained (CDN assets, no build step) so generated
    admin-core CRUD pages render immediately. Replace the CDN links with your
    own Vite/AdminLTE build when you are ready — the only hard contract this
    layout must keep is the four hooks the generated views rely on:
      @yield('title')  @yield('breadcrumb')  @yield('contents')  @stack('scripts')
--}}
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>@yield('title', 'Admin') &middot; {{ config('app.name') }}</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.5.2/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.8/css/dataTables.bootstrap5.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastr@2.1.4/build/toastr.min.css">
    @stack('styles')
</head>
<body class="bg-body-tertiary">
<div class="d-flex" style="min-height:100vh">
    {{-- Sidebar --}}
    <aside class="bg-dark text-white p-3" style="width:240px">
        <a href="{{ route('admin.dashboard') }}" class="navbar-brand text-white d-block mb-4 fw-bold">
            {{ config('app.name') }}
        </a>
        <ul class="nav nav-pills flex-column gap-1">
            <li class="nav-item">
                <a href="{{ route('admin.dashboard') }}" class="nav-link text-white">
                    <i class="fas fa-gauge me-2"></i> Dashboard
                </a>
            </li>
            {{-- Add generated resources here, e.g.:
            <li class="nav-item">
                <a href="{{ route('admin.products.index') }}" class="nav-link text-white">
                    <i class="fas fa-box me-2"></i> Products
                </a>
            </li> --}}
        </ul>
    </aside>

    {{-- Content --}}
    <main class="flex-fill">
        <nav class="navbar navbar-expand bg-white border-bottom px-3">
            <span class="navbar-text">@yield('title', 'Admin')</span>
        </nav>

        <div class="p-4">
            <div class="mb-3">@yield('breadcrumb')</div>

            @if (session('success'))
                <div class="alert alert-success">{{ session('success') }}</div>
            @endif

            @yield('contents')
        </div>
    </main>
</div>

<script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.datatables.net/1.13.8/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.13.8/js/dataTables.bootstrap5.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="https://cdn.jsdelivr.net/npm/toastr@2.1.4/build/toastr.min.js"></script>
<script>
    // CSRF for the AJAX delete (and any other non-GET ajax) the CRUD pages fire.
    $.ajaxSetup({ headers: { 'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content') } });
    // Enable Bootstrap tooltips used by the row action buttons.
    document.addEventListener('DOMContentLoaded', () => {
        document.querySelectorAll('[data-bs-toggle="tooltip"]').forEach(el => new bootstrap.Tooltip(el));
    });
</script>
@stack('scripts')
</body>
</html>
