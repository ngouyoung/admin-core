<!DOCTYPE html>
<html lang="en" data-bs-theme="light">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>@yield('title', '__Portal__ Portal')</title>
    {{-- Load the built admin-core bundle (jQuery, DataTables, select2, SweetAlert, theme) when it's
         available, so resources generated into this portal work just like the admin ones. Otherwise
         fall back to CDN Bootstrap for basic styling. Run `npm install && npm run build`. --}}
    @if (file_exists(public_path('hot')) || file_exists(public_path('build/manifest.json')))
        @vite(['resources/js/app.js'])
    @else
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    @endif
    @stack('style')
</head>
<body>
<div class="d-flex">
    <aside class="ac-sidebar bg-dark text-white p-3" style="width: 250px; min-height: 100vh;">
        <h2 class="h5 mb-4">__Portal__</h2>
        {{-- Data-driven menu for this portal — filtered by the __portal__ guard's permissions. --}}
        <ul class="ac-nav nav flex-column gap-1">
            <x-admin-core::sidebar-menu menu="__portal__" guard="__portal__" />
        </ul>
        <form method="POST" action="{{ route('__portal__.logout') }}" class="mt-4">
            @csrf
            <button class="btn btn-outline-light btn-sm w-100">Log out</button>
        </form>
    </aside>
    <main class="flex-fill p-4">
        @hasSection('breadcrumb')
            <div class="mb-3">@yield('breadcrumb')</div>
        @endif
        @foreach (['success' => 'success', 'error' => 'danger', 'warning' => 'warning', 'info' => 'info'] as $flash => $variant)
            @if (session($flash))
                <div class="alert alert-{{ $variant }}">{{ session($flash) }}</div>
            @endif
        @endforeach
        @yield('contents')
    </main>
</div>
@stack('scripts')
</body>
</html>
